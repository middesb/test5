- pipeline: pipeline2
  events:
  - type: PUSH
    refs:
    - refs/heads/main
  fail_on_prepare_env_warning: true
  actions:
  - action: inialize
    type: BUILD
    disabled: true
    docker_image_name: buddy/localshell
    docker_image_tag: ubuntu_24.04
    execute_commands:
    - ""
    - sleep 5
    - echo "Initialize.."
    shell: BASH
